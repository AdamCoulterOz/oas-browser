@* The whole app, for now. It exists to make the build depend on keel: these
   components come from the package, so if Keel restores but is not usable the
   compile fails here rather than at runtime. *@

<KeelCard Variant="CardVariant.Outlined" Padding="Padding.Md">
    <h1>OAS Browser</h1>
    <p>Scaffold. <KeelBadge Tone="Tone.Good" Dot="true">keel wired up</KeelBadge></p>
</KeelCard>
