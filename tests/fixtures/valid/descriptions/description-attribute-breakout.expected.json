{
  "fixture": "description-attribute-breakout.json",
  "exercises": "a link title breaking out of its quoting, markup inside a code span and a fenced block, and an unclosed attribute that would swallow the rest of the document",
  "validates": true,
  "note": "These documents are valid OpenAPI. Nothing in them is malformed, which is the point: a validator will never flag any of it, so validity is no defence. The hostile part is the content of a description string, and description strings are rendered as HTML.\n\nThis matters now because of the extraction. While the browser read one corpus of first-party specs from the repository that generated them, spec content and application code had the same author and the same trust level. A general browser loads a catalogue from a URL, so spec content becomes arbitrary third-party input rendering into the DOM on the browser's own origin. That change arrives as a consequence of the split rather than as a change anyone makes, and it is silent.\n\nEvery assertion here is written as what must be true. None of them was checked against the current implementation, deliberately: a fixture that recorded today's behaviour would entrench it.\n\nEscaping is context-dependent, and these are four different contexts. Text inside an attribute value, text inside a code span, text inside a fenced block and text in the document body each need different treatment, and a single pass of entity replacement gets at least one of them wrong.\n\nThe code span and fenced block are the case that catches over-correction as well as under-correction: their contents must be displayed literally, as characters a reader can see and copy, and must not be parsed as markup. Both failure directions are wrong, and only one of them is dangerous.\n\nThe unclosed attribute tests that a description cannot damage the page around it. Markup that runs past the end of a description and consumes the elements after it is a defacement even when nothing executes.",
  "security": true,
  "doNotSoften": "Assertions with kind `security` are safety requirements, not rendering preferences. They are written as what must be true. They may be satisfied, and the fixture may then be extended, but they must never be relaxed, narrowed or deleted to match what an implementation currently does.",
  "pending": true,
  "doNotChangeFixture": "Assertions marked pending describe correct behaviour the browser does not have yet. A failure on one of them is a backlog item, not a fixture defect. Do not edit this fixture or its expectations to match what the app currently does.",
  "expected": [
    {
      "assert": "No breakout succeeds. window.__fixtureBreakoutFired is undefined after render and after hovering everything.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    },
    {
      "assert": "The markup inside the code span and inside the fenced block is displayed as literal text, not parsed, and not executed.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    },
    {
      "assert": "A description cannot emit unbalanced markup that escapes its own container. The sentence after the unclosed attribute renders normally, and so does everything after the description.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    },
    {
      "assert": "Rendered output is well-formed regardless of what a description contains: no unclosed element from a description can leave the rest of the page inside it.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    }
  ]
}
