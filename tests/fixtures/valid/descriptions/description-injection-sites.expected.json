{
  "fixture": "description-injection-sites.json",
  "exercises": "the same three-part payload written into all ten places a description can appear, each labelled with its site",
  "validates": true,
  "note": "These documents are valid OpenAPI. Nothing in them is malformed, which is the point: a validator will never flag any of it, so validity is no defence. The hostile part is the content of a description string, and description strings are rendered as HTML.\n\nThis matters now because of the extraction. While the browser read one corpus of first-party specs from the repository that generated them, spec content and application code had the same author and the same trust level. A general browser loads a catalogue from a URL, so spec content becomes arbitrary third-party input rendering into the DOM on the browser's own origin. That change arrives as a consequence of the split rather than as a change anyone makes, and it is silent.\n\nEvery assertion here is written as what must be true. None of them was checked against the current implementation, deliberately: a fixture that recorded today's behaviour would entrench it.\n\nEvery payload writes the name of its own site into window.__fixtureSiteFired, so a failure names the call site that is missing a sanitiser rather than only saying that one exists. Search the rendered page for INJECTION-SITE to enumerate the sites that actually render at all: a site that renders nothing needs no sanitiser today but will the moment someone displays it, and it should be listed rather than assumed safe.\n\nThe info.description site is deliberately absent from this document and is covered nowhere in this set. It is the one description a reader sees before choosing anything, so it is worth adding once the question of where descriptions render has an answer.",
  "security": true,
  "doNotSoften": "Assertions with kind `security` are safety requirements, not rendering preferences. They are written as what must be true. They may be satisfied, and the fixture may then be extended, but they must never be relaxed, narrowed or deleted to match what an implementation currently does.",
  "pending": true,
  "doNotChangeFixture": "Assertions marked pending describe correct behaviour the browser does not have yet. A failure on one of them is a backlog item, not a fixture defect. Do not edit this fixture or its expectations to match what the app currently does.",
  "expected": [
    {
      "assert": "window.__fixtureSiteFired is undefined after render, after hovering everything, and after expanding every collapsed section.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    },
    {
      "assert": "Every site is covered. A sanitiser applied at one call site and missed at another is the expected shape of a partial fix, and this document is arranged to expose exactly that.",
      "status": "pending",
      "kind": "security",
      "why": "Deliberately not assessed against the current implementation. Held pending so it is treated as unmet until someone demonstrates otherwise, rather than being read off what the app happens to do today."
    },
    {
      "assert": "Each of the ten sites renders its description somewhere reachable, so INJECTION-SITE can be found once per site.",
      "status": "pending",
      "why": "Some of these sites may not be displayed at all today. A description that renders nowhere is a separate gap, and it hides the injection question rather than answering it."
    },
    {
      "assert": "The literal marker text renders at every site that displays a description, so the reader can tell a neutralised payload from a dropped description.",
      "status": "implemented"
    }
  ]
}
